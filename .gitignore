.env
.env.deploy

# uv keeps the environment reproducible from uv.lock, which IS tracked
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# go-task's checksum cache
.task/

# Persisted room state (queues, rotation, chat) - runtime data, not source
room-state.json
.state-*.tmp
