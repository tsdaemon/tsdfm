.env
.env.deploy

# uv keeps the environment reproducible from uv.lock, which IS tracked
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# go-task's checksum cache
.task/

# Persisted room state (queues, rotation, chat) - runtime data, not source
room-state.json
.state-*.tmp

.claude/worktrees
app/cover-cache

# Generated DJ break clips (runtime data), and the LLM prompt - gitignored so
# friends' names / inside jokes stay private. See dj-break-prompt.txt.example.
dj-break-cache/
dj-break-prompt.txt
